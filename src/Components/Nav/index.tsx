import { Text, View } from 'react-native';

export const Nav = () => {
    return(
        <View className="w-full h-10 border-black">
            <Text className="text-white">Isso faz parte do NAV</Text>
        </View>
    )
}